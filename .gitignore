node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Stella runtime state (DB + media live here)
/data
drizzle/*.db

# Env
.env
.env.*
!.env.example
!.env.test

# Deployment specifics belong to the machine, not the repo: docker-compose.yml stays the
# generic example and anything site-specific goes in an override beside it.
docker-compose.override.yml

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/test-results
/playwright-report
/blob-report

# Local Claude Code settings (per-machine, never shared)
.claude/settings.local.json

# Per-feature git worktrees (the working agreement puts them here)
.claude/worktrees/
